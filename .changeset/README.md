# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a change. Each changeset is a small markdown file describing a
user-facing change and the semver bump it requires; the changelog and version bumps are generated
from these on release.
